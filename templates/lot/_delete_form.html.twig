<form method="post" action="{{ path('app_lot_delete', {'id': lot.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet enregistrement ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lot.id) }}">
    <button class="btn">Delete</button>
</form>
