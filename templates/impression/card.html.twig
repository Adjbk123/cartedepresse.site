<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap" rel="stylesheet">
    <title>Carte De Presse | {{ carte.professionnel.nom }} {{ carte.professionnel.prenoms }} - {{ carte.numDemande }}</title>
    <link rel="shortcut icon" href="{{ asset('images/favicon.png') }}">


    <style>
        @page  {
            margin:0;
            padding:0;
            width: 85.60mm;
            height:53.98mm;
        }
        .page {
            page-break-after: always;

        }
        body {
            font-family: 'Montserrat', sans-serif;
            background-image:  url("{{ absolute_url(asset('assets/motif.png')) }}");
            background-size: cover;
            background-position: left top;
            background-repeat: no-repeat;

        }

        table {
            width: 100%;
            height: 100%;
            border-collapse: collapse;
        }

        td, th {
            border: 1px solid #ffffff; /* Ajoutez une bordure si nécessaire */
        }

        td div {
            font-size: 10pt;
        }


        .section-title {
            text-align: center;
            font-size: 22px;
            font-weight: bold;
        }

        .card-title {
            text-align: center;
            font-size: 20px;
            font-weight: bold;
            color: red;
            margin-top: 25px;
        }

        .highlight {
            color: #11832b;
            font-size: 13px;
            font-weight: bold;
        }
        .bottom-text {
            background-color: green;
            color: white;
            font-weight: bold;
            border-bottom-left-radius: 10px;
            border-top-left-radius: 10px;
            font-size: 26px;
            text-align: center;
            margin-left: 10px;
        }

        .page {
            page-break-after: always;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding-top: 15px;
        }

    </style>
</head>
<body>
   <div class="page">
       {{ include('impression/card_front.html.twig') }}
   </div>
    <div>
        {{ include('impression/card_back.html.twig') }}
    </div>
</body>
</html>
