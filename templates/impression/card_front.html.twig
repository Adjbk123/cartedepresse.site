
<div>
    <table>
        <tr>
            <td style="width: 20%;text-align: start;vertical-align: middle; ">
                <img src="{{ absolute_url(asset('assets/images/logo-haac.png')) }}" alt="logo" height="20%"> <br>
                <span class="highlight">{{ profession.profession.libelle }}</span>
            </td>
            <td style="width: 60%; margin: 0; ;text-align: center;vertical-align: middle;">
                <span class="section-title">REPUBLIQUE DU BENIN</span> <br>
                <span>HAUTE AUTORITE DE L'AUDIOVISUEL ET DE LA COMMUNICATION</span>
                <br>
                <span class="card-title">CARTE DE PRESSE</span>
            </td>
            <td style="width: 20%; padding-top: 15px;text-align: end;vertical-align: middle;">
                <img src="{{ absolute_url(asset('assets/images/logo-benin.png')) }}" alt="logo" height="40%"> <br>
                <span class="" style=" color: #11832b;   font-size: 13px; font-weight: bold;">{{ profession.organe.designation }}</span>
            </td>
        </tr>
        <tr>
            <td colspan="3" style="margin: 0; text-align: start;">
               <img src="{{ absolute_url(asset('assets/logo-horizontal.png')) }}" alt="" style="max-width: 150mm; max-height: 100%;">
            </td>
        </tr>
        <tr>
            <td style="width: 20%;  ">
                <img src="{{ absolute_url(asset('uploads/')) }}{{ carte.professionnel.photo }}" width="100%"
                     alt="photo">
                <div style=" font-weight: bolder;">{{ carte.numDemande }}</div>
            </td>
            <td style="width: 80%; margin: 0; padding-left: 10px; text-align: left;" colspan="2">
                    <div><strong>NPI / NPIR :</strong> {{ carte.professionnel.npi }}</div>
                    <div><strong>Nom :</strong> {{ carte.professionnel.nom }}</div>
                    <div><strong>Prénoms :</strong> {{ carte.professionnel.prenoms }}</div>
                    <div><strong>Sexe :</strong> {{ carte.professionnel.sexe }}</div>
                    <div><strong>Né(e) le :</strong> {{ carte.professionnel.dateNaissance|date('d/m/Y') }} à {{ carte.professionnel.lieuNaissance }}</div>
                    <div><strong>Nationalité :</strong> {{ carte.professionnel.nationalite }}</div>
                    <div style="padding-bottom: 1px"><strong>Adresse :</strong> {{ carte.professionnel.adresse ? carte.professionnel.adresse : "Cotonou" }}</div>
                <div class="bottom-text">
                    LAISSEZ PASSER
                </div>
            </td>
        </tr>
    </table>

</div>
