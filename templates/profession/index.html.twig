{% extends 'base.html.twig' %}

{% block title %}Listes des professions{% endblock %}
{% block breadcrumb %}Profession{% endblock %}
{% block breadcrumbActive %}Liste{% endblock %}
{% block datatables %}{% endblock %}

{% block body %}


    <table class="table table-hover table-bordered myTable">
        <thead>
            <tr>
                <th>N°</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for profession in professions %}
            <tr>
                <td>{{ loop.index }}</td>
                <td>{{ profession.libelle }}</td>

                <td>
                    <div class="dropdown mt-4 mt-sm-0">
                        <a href="#" class="btn btn-outline-light btn-sm dropdown-toggle border " data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="mdi mdi-dots-horizontal"></i>
                        </a>

                        <div class="dropdown-menu" style="">
                            <a class="dropdown-item link-info" href="{{ path('app_profession_show', {'id': profession.id}) }}">Voir</a>
                            <a class="dropdown-item link-primary" href="{{ path('app_profession_edit', {'id': profession.id}) }}">Editer</a>
                            {{ include('profession/_delete_form.html.twig') }}
                        </div>
                    </div>
                </td>

            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_profession_new') }}">Create new</a>
{% endblock %}
