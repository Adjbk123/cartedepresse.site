<form method="post" action="{{ path('app_type_organe_delete', {'id': type_organe.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cet enregistrement ? ');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_organe.id) }}">
    <button class="dropdown-item link-danger">Supprimer</button>
</form>
