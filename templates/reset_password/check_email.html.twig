{% extends 'baseAccueil2.html.twig' %}

{% block title %}Lien de réinitialisation envoyé{% endblock %}

{% block body %}
    <!-- Hero Start -->
    <section class="bg-half-100 bg-light d-table w-100">
        <div class="container">
            <div class="row mt-5 justify-content-center">
                <div class="col-lg-12 text-center">
                    <div class="pages-heading">
                        <h4 class="title mb-0">Lien de réinitialisation envoyé</h4>
                    </div>
                </div>
            </div>

            <div class="position-breadcrumb">
                <nav aria-label="breadcrumb" class="d-inline-block">
                    <ul class="breadcrumb rounded shadow mb-0 px-4 py-2">
                        <li class="breadcrumb-item"><a href="{{ path('app_accueil') }}">Accueil</a></li>
                        <li class="breadcrumb-item active" aria-current="page">E-mail envoyé</li>
                    </ul>
                </nav>
            </div>
        </div>
    </section>
    <!-- Hero End -->

    <section class="section bg-light py-5">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <div class="card border-0 shadow">
                        <div class="card-body p-4">
                            <p class="mb-3">
                                Si un compte correspondant à cette adresse e-mail existe, un message vient d’être envoyé avec un lien pour réinitialiser votre mot de passe.
                            </p>
                            <p class="mb-3">
                                Ce lien expirera dans :
                                <strong>{{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}</strong>.
                            </p>
                            <p>
                                Si vous ne recevez pas de message d’ici quelques minutes, vérifiez votre dossier de spams ou
                                <a class="fw-bold" href="{{ path('app_forgot_password_request') }}">réessayez</a>.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
